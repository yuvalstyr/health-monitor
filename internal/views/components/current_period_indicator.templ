package components

import (
	"time"
)

templ CurrentPeriodIndicator() {
	<div class="bg-base-200 rounded-lg p-3 mb-4 text-center">
		<div class="text-lg font-semibold">Today: { formatCurrentDate() }</div>
	</div>
}

func formatCurrentDate() string {
	return time.Now().Format("Monday, January 2")
}