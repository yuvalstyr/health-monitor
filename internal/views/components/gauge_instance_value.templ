package components

import (
	"fmt"
	"health-monitor/internal/db"
)

templ GaugeInstanceValue(instance *db.GaugeInstance) {
	<div class="gauge-instance-value" id={ fmt.Sprintf("gauge-instance-value-%d", instance.ID) }>
		<span class="text-3xl sm:text-2xl font-bold text-primary">{ fmt.Sprintf("%d", instance.Value) }</span>
	</div>
}